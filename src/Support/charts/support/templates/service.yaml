apiVersion: v1
kind: Service
metadata:
  name: {{ template "support.fullname" . }}
  labels:
    app: {{ template "support.name" . }}
    chart: {{ template "support.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "support.name" . }}
    release: {{ .Release.Name }}
